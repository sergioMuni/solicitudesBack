export enum Rol {
    Administrador = 1,
    Consultor,
  }
  
  export enum TipoPedido {
    Calzada = 1,
    Catastro,
    
  }
  
  export enum Estado {
    Iniciada = 1,
    Autorizada,
    Rechazada,
    Pagada,
  }
  